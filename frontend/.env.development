# VITE_BACKEND_URL is the URL of the backend server for the development environment.
VITE_BACKEND_URL=http://localhost:8080

# VITE_FRONTEND_URL is the URL of the frontend application for the development environment.
VITE_FRONTEND_URL=http://localhost:3000
